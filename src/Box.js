import React, { Component } from 'react';

const Box = (props) => {
    return (<div className="text-box">
                <input className="text-input "type="text" />
            </div>);
}

export default Box;